/* DESKTOP-ONLY parse-stub of BSP <cdef21569.h> for the M1 guard-region syntax check.
 * NOT the real BSP header (the real one is a huge register-definition file). Empty stub: m1_sru.c
 * includes it for completeness but uses only pREG_* (from sys/platform.h) and SRU2 (from sru21569.h). */
#ifndef M1_MOCK_CDEF21569_H
#define M1_MOCK_CDEF21569_H
#endif
