/* DESKTOP-ONLY parse-stub of BSP "adi_initialize.h" for the M1 main guard-region syntax check.
 * NOT the real BSP header. Resolves adi_initComponents() (the generated board-init entry). */
#ifndef M1_MOCK_ADI_INITIALIZE_H
#define M1_MOCK_ADI_INITIALIZE_H
static inline int adi_initComponents(void) { return 0; }
#endif
