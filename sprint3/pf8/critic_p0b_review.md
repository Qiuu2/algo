# Critic 复核 — P0-B 资产盘点三档分类逻辑

**评审对象**：`sprint3/pf8/p0b_asset_inventory.md`（PM 作者）
**评审范围**：仅复核**三档分类判定逻辑**（非 grep 准确性）。**不复核 P0-A-1 栅瓣判据**（已由 PM 独立复算确认）。
**评审员**：Critic ｜ **日期**：2026-05-29 ｜ **深度**：spot_check（紧凑任务）

---

## 裁决：PASS_WITH_MINOR ｜ Confidence: HIGH

分类逻辑**可信**，三档定义互斥且穷尽，P1-A 工作量可据此估算。#14 走重审而非 ESCALATE#2 **认同**。**无漏网活文档**。**无 BLOCKER**。仅 1 项 MINOR（路径笔误）。

---

## 1. 三档逻辑结论
三档定义（§1）**合理**：
- 判据轴选得对 = "**当前是否活跃/可作决策依据/可编译**"，而非简单按文件类型。
- 三档**互斥**（活跃→重审 / 历史结论→作废 / 巧合或已正确标注→不影响），且对 31 处资产**穷尽无重叠**。
- "作废=加标不删"符合可追溯原则（历史结论须留痕，避免误判为遗漏）。
- 边界处置一致：同类历史脚本（#17/#18/#19）统一归"作废"，无分级漂移。

## 2. #14 C 代码判定（重点核）
**结论：认同 PM 判定（走 P1-A 重审/重生成，不走 ESCALATE#2）。**
- (a) `sprint3/dsp/tree_filterbank.c`：独立 grep 确认**零** d/间距/spacing/0.030/55mm 引用 → **LOCKED 树形几何无关**成立，d=55 不触碰树形核。
- (b) `ARRAY_ELEMENT_SPACING_MM 30u`（config.h:59）**确在废弃全速率模板**：
  - 实际消费点确认 = `beamformer.c:107  float d_m = ARRAY_ELEMENT_SPACING_MM/1000.0f`（PM 引"bf:107"准确）。
  - 缓冲深度确认 = `MAX_INT_DELAY_SAMPLES 54u`（config.h:95），注释显式 `15×0.030×0.707/343×48000+10=54`，确按 d=0.030 算；d=55 须重算（PM 估 ~92 合理量级）。
  - 模板性质确认 = config.h:80-87 显示全速率参考核（437 抽头）已让位于 dyadic 树形半带核（63 抽头），**全速率模板为废弃路径、非 LOCKED、无产物编译**。
- 三要素（废弃模板 / 非 LOCKED / 无产物）齐备 → **ESCALATE#2 不成立**，常规清理正确。残留清除须 dsp 重生成 + Critic 对抗复核（PM §3 已要求，认可）。

## 3. 三边界抽查
- **#25** `hardware-design/skill.md:494/512/514`：确认 30mm = 散热片包络 `80×60×30mm max` + 高度范围 `15-30mm/>30mm`，**非阵元间距** → 不影响成立。
- **#26** `structure/memory.md:507`：确认 `螺钉间距≤30mm`，**非阵元间距** → 不影响成立。
- **#27** POLICY-PROV-001:116：确认 DEC-S2-013 已注 `(已 d=55 重算 P0-3)` + 标 L2 + 指向 DEC-S3-P0-01，**已正确标注迁移** → 不影响成立（措辞已自洽，无需动）。

## 4. 查漏结论
独立扫 `sprint3/` 全量 d=30 命中 = 5 文件，**全部已在 PM 表内**（#10 dsp_8ch_report / #28 sweep_d55.py / #29 sweep_d55_report / #30 critic_review_s3 + 盘点表自身）。**无应"重审"却被错放"作废/不影响"的活文档** → P1-A 不会漏改。

## 5. Findings（C1-C5）
- **[MINOR] C1 路径笔误**：PM 表 #14 与 §0 写 `cces_template/src/config.h`，实际为 `sprint2/dsp/cces_template/src/config.h`。内容判定无误，但 P1-A 按错误路径会找不到文件 → 须修正为完整路径。
- **C2**（INFO）：#14 缓冲深度 d=55 重算值 PM 标 "~92"，属估算占位；重生成时须由 dsp 按 `ceil(15×0.055×0.707/343×48000)+10` 实算并由 Critic 复核（已纳入 §3 流程，记此备查）。
- **C3**（INFO）：分类逻辑判据轴（活跃性）选得准，给予认可。
- **C4**（INFO）：作废档"加标不删"原则正确，利于可追溯。
- **C5**（INFO）：d=55 为 CTO 硬锁，本盘点全程未触发"重议 d=55"，无 ESCALATE。

---

*Critic P0-B 复核，2026-05-29。裁决 PASS_WITH_MINOR（仅 C1 路径笔误待修，不阻塞 P1-A 启动）。*
